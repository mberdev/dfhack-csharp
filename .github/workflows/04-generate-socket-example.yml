name: 04 (TRIGGERED) Generate Socket Example Project

on:
  workflow_run:
    workflows: ["03 (TRIGGERED) Generate NuGet Packages"]
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-example:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Ensure branch is up to date
        run: |
          set -euo pipefail
          set -x
          git fetch origin main
          git checkout main
          git pull origin main --rebase

      - name: Install .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Copy HandshakeExample template and add to solution
        run: |
          set -euo pipefail
          set -x

          for ROOT in dfhack-v*; do
            [[ -d "$ROOT" ]] || continue
            echo "Processing $ROOT..."

            CSHARP_DIR="$ROOT/csharp"
            DEST="$CSHARP_DIR/HandshakeExample"

            cp -r templates/HandshakeExample "$DEST"

            SOLUTION_FILE=$(ls "$CSHARP_DIR"/dfhack-csharp.sln* | head -n 1)
            echo "Adding HandshakeExample to solution: $SOLUTION_FILE"
            dotnet sln "$SOLUTION_FILE" add "$DEST/HandshakeExample.csproj"
          done

      - name: Commit HandshakeExample
        run: |
          set -euo pipefail
          set -x
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          git add dfhack-v*/csharp/HandshakeExample/
          git add -f dfhack-v*/csharp/*.sln*

          git commit -m "Generated HandshakeExample console app" || echo "No changes to commit"
          git push
